<h1><%= @deck.name %></h1>
<div>
  <a href="/decks/<%= @deck.id %>/cards/new">Add a card</a>
<div>
<br>
<% @cards.each do |card| %>
  <div>
    <div>
      <%= card.front %>
    </div>
    <div>
      <%= card.back %>
    </div>
  </div>
  <br>
<% end %>
